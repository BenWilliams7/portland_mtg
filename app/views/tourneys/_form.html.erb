<%= form_with(model: tourney, local: true) do |form| %>
  <% if tourney.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tourney.errors.count, "error") %> prohibited this tourney from being saved:</h2>

      <ul>
      <% tourney.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :tourney_name %>
  </div>

  <div class="field">
    <%= form.label :start_time %>
    <%= form.datetime_select :start_time, id: :tourney_start_time %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
